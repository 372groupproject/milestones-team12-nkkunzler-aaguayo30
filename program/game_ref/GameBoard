GameBoard

NAME:
	_gen_gameboard

SYNOPSIS:
	%include "gameboard_gen.asm"
	
	GameBoard* _gen_gameboard(WINDOW*, char* map_file, int file_size, int x, int y)

DESCRIPTION:
	A GameBoard struct is defined as being:

	struct GameBoard {
		WINDOW* window;
		Player* player;
		Player* enemy;
	}

RETURN:
	Returns a GameBoard struct which contains the window in which the gameboard is loaded to, the player, and an enemy.

